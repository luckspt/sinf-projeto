# Script to flush iptables rules

# Set default chain policies to DROP for incoming network traffic
sudo iptables -A INPUT DROP

# Set default chain policies to DROP for incoming network traffic
sudo iptables -A OUTPUT DROP